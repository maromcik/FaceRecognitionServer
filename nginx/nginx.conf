upstream gunicorn {
    server app:8000;
}

server {

    listen 80;

    location / {
        proxy_pass http://gunicorn;
    }

    location /static/ {
        alias /var/www/static/;
    }

    location /media/ {
        alias /var/www/media/;
    }

}

